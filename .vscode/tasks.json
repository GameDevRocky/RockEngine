{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "RockEngine: configure (qt-msvc-debug)",
            "type": "shell",
            "command": "cmake --preset qt-msvc-debug",
            "problemMatcher": [],
            "detail": "Configure with the MSVC + Qt 6.11.0 CMake preset."
        },
        {
            "label": "RockEngine: build (qt-msvc-debug)",
            "type": "shell",
            "command": "cmake --build --preset qt-msvc-debug",
            "dependsOn": ["RockEngine: configure (qt-msvc-debug)"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$msCompile",
            "detail": "Build with the MSVC + Qt 6.11.0 CMake build preset."
        }
    ]
}