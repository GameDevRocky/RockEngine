Globals:
  Rendering:
    useGPUNormalMapGeneration:
      type: bool
      value: true
      description: Generate texture normal maps with an OpenGL compute shader. Falls back to CPU when unavailable.
